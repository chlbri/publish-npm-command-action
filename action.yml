name: 'Your name here'
description: Template for future gh-actions
author: chlbri
inputs:
  path: # change this
    required: false
    description: Path of the package.json
    default: 'default value if applicable'
outputs:
  version:
    description: The next version of the package
  command:
    description: the npm publish command

runs:
  using: 'node16'
  main: 'build/index.js'
